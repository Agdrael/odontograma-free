# odontograma-free
Odontogram SDK is a modular TypeScript library for building interactive dental charts. It provides a framework-agnostic core for tooth states, numbering systems and clinical events, with optional UI packages for modern web applications.
